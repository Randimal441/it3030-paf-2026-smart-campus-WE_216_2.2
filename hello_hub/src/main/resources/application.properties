spring.application.name=hello_hub

# MongoDB
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://pafuser:12345%40@cluster0.3ydsgb7.mongodb.net/smart-campus?retryWrites=true&w=majority&appName=Cluster0}

# JPA datasource (dev default)
# Use a file-backed H2 database so resources persist across application restarts.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:h2:file:./data/smartcampus;MODE=MySQL;AUTO_SERVER=TRUE}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.h2.Driver}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sa}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.H2Dialect}
spring.h2.console.enabled=${SPRING_H2_CONSOLE_ENABLED:true}

# Google OAuth2 client registration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:47969746058-hdgur1uftn3cl8iaksj3k17shfrt6p83.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=profile,email

# Application security
app.frontend.redirect-uri=${APP_FRONTEND_REDIRECT_URI:http://localhost:5173}
app.jwt.secret=${APP_JWT_SECRET:replace-this-with-a-very-long-and-random-secret-key-at-least-32-bytes}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}
